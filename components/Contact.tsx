import React from 'react';

// This component is no longer used in the Bible App.
// It is kept to prevent breaking the build process if imported elsewhere,
// but it renders nothing.
const Contact: React.FC = () => {
  return null;
};

export default Contact;